---
import Section from "@/components/Section.astro"
import { formatCvDate } from "@/i18n/formatCvDate"
import { cvLabels } from "@/i18n/ui"

interface Education {
  institution: string
  startDate: string
  endDate: string | null
  area: string
  studyType: string
  url: string
}

const education: Education[] = Astro.props.cv.education
const currentLanguage = (Astro.currentLocale ?? "en") as "en" | "es"
const labels = cvLabels[currentLanguage]
---

<Section title="education">
  <ul>
    {
      education.map(
        ({ institution, startDate, endDate, area, studyType, url }) => {
          const start = formatCvDate(startDate, currentLanguage)
          const end = endDate
            ? formatCvDate(endDate, currentLanguage)
            : labels.present
          const dates = `${start} - ${end}`
          const degree = studyType
            ? `${studyType} ${labels.educationIn} ${area}`
            : area

          return (
            <li>
              <article data-print-item="education">
                <header>
                  <div>
                    <h3>
                      <a
                        href={url}
                        title={institution}
                        target="_blank"
                        rel="noopener noreferrer"
                      >
                        {institution}
                      </a>
                    </h3>
                  </div>

                  <time>{dates}</time>
                </header>

                <footer>
                  <p>{degree}</p>
                </footer>
              </article>
            </li>
          )
        },
      )
    }
  </ul>
</Section>

<style>
  ul {
    display: flex;
    flex-direction: column;
    gap: 32px;
  }

  article h3 {
    font-weight: 500;
    color: var(--text-heading);
  }

  article h3 {
    text-wrap: balance;
  }

  article a {
    color: var(--text-heading);
    display: inline;
  }

  article a:hover {
    text-decoration: underline;
  }

  /* Institution and dates share a row only while both really fit: the
     title column can wrap without pushing the period out of the viewport. */
  header {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    column-gap: 16px;
    align-items: baseline;
    margin-bottom: 4px;
  }

  header > div {
    min-width: 0;
  }

  time {
    color: var(--text-muted);
    font-size: 0.85rem;
    white-space: nowrap;
    text-align: right;
  }

  @media screen and (width <= 560px) {
    header {
      grid-template-columns: minmax(0, 1fr);
      row-gap: 4px;
    }

    time {
      text-align: left;
    }
  }

  @media print {
    ul {
      display: block;
    }

    li + li {
      margin-top: 10pt;
    }

    article {
      break-inside: avoid-page;
      page-break-inside: avoid;
    }

    article h3 {
      font-size: 10.25pt;
      line-height: 1.2;
    }

    article footer p {
      font-size: 8.75pt;
      line-height: 1.3;
    }

    header {
      margin-bottom: 1.5pt;
    }

    time {
      min-width: 118pt;
      font-size: 8.25pt;
      line-height: 1.3;
      text-align: right;
    }
  }
</style>
